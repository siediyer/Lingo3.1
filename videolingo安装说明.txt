
.venv\Scripts\activate
streamlit run st.py

python -c "import torch; print(torch.__version__); print('cuda:', torch.cuda.is_available()); print('count:', torch.cuda.device_count())"
python -c "import torch; print(torch.version.cuda)"
nvidia-smi

chcp 65001
set PYTHONUTF8=1
setx PYTHONUTF8 1


python -c "import torch; print(torch.__version__); print('cuda:', torch.cuda.is_available()); print('count:', torch.cuda.device_count()); print(torch.version.cuda)"


cd /d C:\videolingo
deactivate 2>nul
rmdir /s /q .venv
python -m venv .venv
.venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -U pip setuptools wheel
python -m pip install -r requirements.txt
python install.py
python -m streamlit run st.py